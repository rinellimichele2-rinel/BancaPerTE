# Admin password per accesso panel amministrativo
ADMIN_PASSWORD=admin123

# Porta server (Render usa PORT automaticamente, default 5000)
PORT=5000

# Ambiente (production per Render)
NODE_ENV=production

# URL del database PostgreSQL (fornito da Render)
# Esempio: postgresql://user:password@host.region.render.com:5432/database
DATABASE_URL=postgresql://user:password@host:5432/database

# URL pubblico dell'API (il tuo URL Render dopo il deployment)
# Esempio: https://bank-interface-flow.onrender.com
EXPO_PUBLIC_API_URL=http://localhost:5000

# OpenAI API Key per il consulente AI (opzionale)
OPENAI_API_KEY=your_openai_api_key_here

# Domini Replit (se usi Replit, altrimenti ignora)
# REPLIT_DEV_DOMAIN=
# REPLIT_DOMAINS=
# EXPO_PUBLIC_DOMAIN=